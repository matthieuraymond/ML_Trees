function [ res ] = testTrees( T, x )
%PREDICTION output a vector of prediction for the x data matrix
    
     [~, n] = size(x);

     for i = 1 : (1 + ending - starting)
        predicted = predict(T, testSet(i,:));


end

